updated_at: "2026-06-28T14:31:44-04:00"
active_task: "neval-telemetry-integrity-layer"
status: "completed"
notes:
  - "Telemetry now distinguishes measured values from missing or incomplete instrumentation instead of presenting all tok/s and tool metrics as equally authoritative."
  - "Run detail now exposes a confidence layer that separates pass rate from proof coverage, known-bad coverage, and structural weaknesses."
  - "Run detail output now prioritizes live eval readability, visual artifact previews, and responsive operator UI."
  - "Mobile layout is no longer intentionally deprioritized for run detail; sidebar collapses to a top rail on narrow viewports."
  - "GLM 5.2 should be treated as vision-input blind but strong at visual-code output such as SVG, Three.js, web UI, and app UI."
  - "Private GitHub repo created at RasputinKaiser/NEval."
  - "Telemetry strip surfaces errorRate, forbiddenViolationRate, failsSafelyRate, and cheapestPassUsd"
  - "UX pass mounted advertised routes instead of leaving sidebar links as 404s."
  - "Visual-code is now part of the active case universe, not just files on disk."
receipts:
  - "initial browser review found compare turns delta, nav highlighting, hydration warning, and mobile-only layout issues"
  - "npm run lint passed"
  - "npm run selftest passed: 7 pass, 0 fail, 6 skip"
  - "npm run build passed"
  - "npm run typecheck passed after Next generated types settled"
  - "focused grader timeout probe returned passed=false for sleep 2 with timeout_ms=100"
  - "browser QA verified no framework overlay, corrected compare turns delta, static New Run placeholder, dashboard bench links, and exact sidebar active states"
  - "accuracy audit route and CLI added"
  - "model capabilities split visionInput from visualCodeOutput"
  - "oracle coverage expanded to 13/13 cases"
  - "selftest passed 13/13 with no skips"
  - "private origin configured as https://github.com/RasputinKaiser/NEval"
  - "New Run, Compare, bench, case detail, and run POST API routes mounted"
  - "dashboard total runs now uses DB count and test cases reports 18"
  - "selftest passed 18/18 with no no-oracle skips; 6 LLM-judge skips remain gated"
  - "accuracy audit reports 18/18 oracle coverage, 5 known-bad scripts, and 2 visual contracts"
  - "npm run build passed and regenerated Next route types"
  - "npm run typecheck passed after build-generated types settled"
  - "browser QA on localhost:3001 verified dashboard, New Run prefill, Compare, bench, case detail, and Accuracy routes render without 404 or error overlay"
  - "POST /api/runs returns clean 400 JSON for impossible filters"
  - "dev server on localhost:3001 was restarted after next build invalidated the live .next chunk cache"
  - "run detail now shows a visual run header, progress summary, per-case preview badges, and inline sandboxed SVG/HTML artifact previews"
  - "Browser QA on localhost:3001/runs/39e17453 verified SVG status-card.svg renders inline with no console errors"
  - "Browser QA on localhost:3001/runs/39e17453 verified index.html renders with styles.css inlined and visible Pass@1, Pass@k, Known-bad, and Visual-code labels"
  - "Browser viewport QA verified 1440x1000 desktop and 390x844 mobile render readable run output after responsive sidebar/top-rail update"
  - "npx tsc --noEmit and npm run lint -- --max-warnings=0 passed"
  - "run detail confidence score added from existing case metadata and grader evidence tiers without schema migration"
  - "case rows now show compact trust scores; selected case shows oracle, known-bad, proof, budget, visual contract, deterministic, trace, and weakness signals"
  - "grader section now groups evidence by deterministic, trace, visual, LLM judge, and manual tiers before raw grader rows"
  - "Browser QA on localhost:3001/runs/39e17453 verified Evaluation confidence, Case trust contract, Evidence groups, and Weakness radar render with no console errors"
  - "Browser viewport QA captured readable confidence layer receipts at 1440x1000 and 390x844"
  - "npx tsc --noEmit and npm run lint -- --max-warnings=0 passed after confidence layer implementation"
  - "parseStreamLine now records tool start timestamps and computes tool duration from streamed tool_use/tool_result pairs for future runs"
  - "runner result normalization now returns endedAt = startedAt + measured runner wall duration for headless and tmux runs"
  - "caseTelemetry now reports durationSource, tokenSource, toolSource, throughputMode, toolDurationCoverage, and warnings"
  - "telemetry API now reuses caseTelemetry for per-case rows instead of duplicating tok/s math and hardcoding msPerTool=0"
  - "TelemetryStrip now shows duration source coverage, usage source coverage, tool timing coverage, and warning chips"
  - "bench telemetry page now shows Telemetry integrity panel, per-case source chips, ms/tool, and warnings"
  - "npm run test:telemetry added and passed, covering parser tool duration and telemetry provenance"
  - "Browser QA on localhost:3001/runs/39e17453 verified telemetry integrity strip renders source coverage and warnings with no console errors"
  - "Browser QA on localhost:3001/runs/39e17453/bench verified Telemetry integrity, Tool events, source, and tool ms surfaces with no console errors"
  - "npx tsc --noEmit and npm run lint -- --max-warnings=0 passed after telemetry integrity implementation"
