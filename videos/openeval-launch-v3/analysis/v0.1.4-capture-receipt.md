# OpenEval v0.1.4 capture receipt

Recorded 2026-07-31 from the local v0.1.4 suite after the evaluation-detail privacy pass.

## Capture set

All clips were recorded from the running local app with the dark theme and default-on display redaction enabled. The recorder viewport is 1920×968 at 30 fps; the final film composes these product clips into the 1920×1080 master.

| Clip | Route | Mode | Duration | Role |
| --- | --- | --- | ---: | --- |
| `01-dashboard-overview.mp4` | `/` | hold | 6 s | product overview |
| `02-live-observation.mp4` | `/live` | hold | 6 s | live observation |
| `03-collection-signal.mp4` | `/collection` | hold | 6 s | collected evidence |
| `04-timeline-trends.mp4` | `/collection/timeline` | hold | 6 s | longitudinal signal |
| `05-new-run-builder.mp4` | `/runs/new` | hold | 6 s | benchmark setup |
| `06-runs-history.mp4` | `/runs` | hold | 6 s | run history |
| `07-leaderboard.mp4` | `/runs/leaderboard` | hold | 6 s | leaderboard |
| `08-compare-runs.mp4` | `/runs/compare?a=f5c15c33&b=c4663fff` | hold | 6 s | run comparison |
| `09-run-confidence.mp4` | `/runs/f5c15c33` | scroll | 10 s | confidence and evidence |
| `10-cases-library.mp4` | `/cases` | scroll | 8 s | case library |
| `11-bench-telemetry.mp4` | `/runs/16f7bc71/bench` | scroll | 8 s | benchmark telemetry |
| `12-accuracy-audit.mp4` | `/accuracy` | scroll | 8 s | accuracy audit |
| `13-visual-markdown-proof.mp4` | `/runs/f5c15c33/case/visual-markdown-runbook` | scroll | 10 s | Markdown artifact proof |
| `14-visual-voxel-proof.mp4` | `/runs/30ce6001/case/visual-isometric-voxel-world` | scroll | 10 s | voxel-world artifact proof |

## Privacy validation

The first capture batch was discarded after an unredacted local username was noticed in evaluation-detail surfaces. The detail route now applies the same display-only redaction path used elsewhere in the suite to case names, transcripts, tool inputs and outputs, artifact previews and paths, grader evidence, final answers, and error text. The raw API payload and stored artifacts remain unchanged.

Targeted browser checks after the fix found no private home-directory matches in the rendered full case view or run-detail view, and confirmed the compact redaction controls are present. The refreshed footage and the contact sheet in this directory are from the post-fix capture batch.

## Capture tooling note

The local CDP recorder had a response-registration race that could hang on fast protocol replies. The pending request is now registered before the message is sent, and all 14 refreshed captures completed successfully.

## Visual review

`v0.1.4-footage-sheet.jpg` is a post-fix contact sheet covering all 14 clips. It was regenerated after the replacement captures and visually inspected before the launch-film render.

## Style revision

The follow-up art-direction pass organizes the cut around one shared editorial system: sharp proof windows with a consistent hairline and shadow, a persistent seven-chapter rail (`PROOF`, `OBSERVE`, `SIGNAL`, `REPEAT`, `COMPARE`, `COVERAGE`, `DECIDE`), and one violet aperture wipe across all six chapter cuts. The chapter rail exits before the final wordmark so the acknowledgment remains the visual endpoint.

The styled master is `renders/video.mp4`. Final verification: 1920×1080, 30 fps, H.264/AAC stereo at 48 kHz, 29.525333 s, 12,575,980 bytes, SHA-256 `6e5c73795cccbff74ed1b7e9281a8afc544cbc8b74cc542359e0149902f85de3`. The approved prior master was preserved as `renders/video-pre-20260731-020816.mp4` before the style revision.

`v0.1.4-style-final-sheet.jpg` is the contact sheet extracted from the final styled MP4. Static preflight, story review, seam review, HyperFrames check, and final release verification passed; the check recorded 124/124 WCAG AA text checks after the rail contrast adjustment.

## Flow revision

The next pass restructured the film around a causal proof chain rather than the order in which the product routes happen to exist: **question → build → observe → signal → compare → coverage → decision**. The repeatable run builder and run history now arrive immediately after the opening evidence claim; live observation follows as the system in motion; collection and timeline turn that observation into a longitudinal signal; visual Markdown and voxel-world outputs then make comparison tangible; coverage and accuracy close the evidence loop before the final decision/telemetry frame and the existing acknowledgment lockup.

The seven-chapter rail now follows that same order (`PROOF`, `REPEAT`, `OBSERVE`, `SIGNAL`, `COMPARE`, `COVERAGE`, `DECIDE`). The reordered scenes use dedicated timeline lanes so the source compositions cannot overlap at the new cut points. Story and seam snapshots were regenerated and visually reviewed at the new entrances: 5.0s, 8.5s, 12.0s, 15.5s, 19.0s, and 22.5s.

The flow-revised master was `renders/video.mp4`. Final verification for that intermediate master: 1920×1080, 30 fps, H.264/AAC stereo at 48 kHz, 29.525333 s, 12,663,582 bytes, SHA-256 `768f3c8084dc9de20b702ed6a984abeb5a06b80ccb2e479f13916a20a90b510d`. The previous styled master was preserved as `renders/video-pre-20260731-022448.mp4` before that render. `v0.1.4-flow-final-sheet.jpg` is the contact sheet from that encoded intermediate master.

## Critique refinement

Three independent reviewers plus a direct encoded-frame review converged on the same refinement: the causal order was right, but the hook repeated the New Run Builder, the COMPARE beat showed an auth-blocked/missing-file failure surface, and the darkest product windows receded at feed scale. The motion review also found a real source-level seam defect at 15.5s: the outgoing transition referenced the repeat scene instead of the signal scene, allowing the prior `history becomes signal` headline to persist underneath the compare entrance. That outgoing scene reference is corrected.

The refinement makes four substantive changes:

- the hook now uses the harness leaderboard as its secondary proof, leaving the New Run Builder and run history to own REPEAT;
- COMPARE uses a derived 960×540 landscape plate from the user-provided observed voxel-world artifact preview, rather than the captured run page that visibly contained `401 OAuth access token has been revoked`, missing `voxel-world.svg`, and failed grader output;
- the compare scene now names its operation with `COMPARE RUNS / INSPECT EVIDENCE / WATCH OUTPUTS`, while the OBSERVE label is more direct (`LIVE USAGE / SESSION INTELLIGENCE`);
- local midtone/opacity treatment was lifted for the darkest proof surfaces, and the COVERAGE headline yields to its three evidence lines late in the beat.

The derived voxel preview is a visual sample only. It preserves the observed artifact’s own “visual quality is not automatically verified” boundary; it is not a provider-backed run result and does not assert pixel-quality success. The original `14-visual-voxel-proof.mp4` remains in the capture set for provenance but is intentionally not used in the final launch master.

The critique-refined canonical master is `renders/video.mp4`. Final verification: 1920×1080, 30 fps, H.264/AAC stereo at 48 kHz, 29.525333 s, 12,177,460 bytes, SHA-256 `54bc55de757078d347757f373f344b16e04525a5e571a3a985a2d5925c344850`. The previous flow-revised master was preserved as `renders/video-pre-20260731-024630.mp4`. `v0.1.4-critique-final-sheet.jpg` is the contact sheet extracted from the encoded canonical MP4, including focused 20.3s and 21.7s coverage frames. Static preflight, story review, seam review, HyperFrames check, and final release verification passed; the final check recorded 117/117 WCAG AA text checks. HyperFrames still reports the project’s known non-blocking Studio editability and sparse-keyframe advisories.

## Sound-structure revision

This pass keeps the approved visual cut and restructures the soundtrack around the film’s actual musical and editorial cadence. The existing `openeval-pulse.v4-tight.mp3` bed was analyzed with the music-to-video beat-grid tool: 161 BPM, 79 beats, 20 bars, five native four-bar phrases (`0.14–5.94s`, `5.94–11.77s`, `11.77–17.60s`, `17.60–23.41s`, and `23.41–29.50s`), a real energy drop around 15s, and a late hard-stop tendency around 26s. The supplied `sfx-mix.wav` was verified as an intentional, non-silent transition-hit stem rather than an empty leftover asset; its larger arrivals land around 1.7s, 6.1s, and 12.7s, with comparison and decision accents at approximately 17.8–18.4s, 20.4s, 23.2s, and 25.4s.

The new `assets/audio/openeval-pulse.v4-structured.mp3` is a 29.5-second stereo 48 kHz premix. It keeps the Pulse bed primary, adds a high-pass/low-pass cleaned transition-hit layer at restrained gain, uses gentle sidechain ducking so the hits have room without flattening the music, fades in over the native opening silence, and releases into a short clean tail after the acknowledgment. There is no voice or narration layer. The final mix measures −15.1 LUFS integrated, 1.6 LU LRA, and −2.9 dBFS true peak; the encoded MP3 is 1,183,001 bytes with SHA-256 `80f60797afae7439ef8981d547494f0cccc67b6ed99301d669a4ca066d70f60b`.

The audio-structured canonical master is `renders/video.mp4`. Final verification: 1920×1080, 30 fps, H.264/AAC stereo at 48 kHz, 29.525333 s, 12,169,249 bytes, SHA-256 `a9bab6423868d92a052a70cfc05b57af92410ed07fdf0d35b71d2f8e0087bb92`. The previous critique-refined master was preserved as `renders/video-pre-audio-20260731-0300.mp4`. The fresh encoded master measures −15.1 LUFS integrated, 1.6 LU LRA, and −3.0 dBFS true peak on its AAC track. `v0.1.4-audio-structure-sheet.jpg` is the 12-frame contact sheet extracted from this final MP4.

Release preflight, story review, seam review, HyperFrames check, and release verification passed. HyperFrames reported 0 errors, 36 known Studio/sparse-keyframe warnings, and 10 informational notices; runtime, motion, and layout had no errors or warnings, and contrast remained 117/117 WCAG AA. The existing visual artifact boundary remains unchanged: the voxel preview is still a visual sample only, not provider-backed or pixel-quality proof.
