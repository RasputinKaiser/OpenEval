# OpenEval — Canonical Feature / User Story Spreadsheet

Evidence date: 2026-07-16. Statuses describe the current checkout, including uncommitted operator-owned changes present during verification.

| ID | Feature | User story | Expected behavior | Test method | Status | Error | Fix | Retest status |
|----|---------|------------|-------------------|-------------|--------|-------|-----|---------------|
| US-001 | Local setup and dashboard start | As a new operator, I install dependencies and start OpenEval so that I can use the local dashboard. | A clean Node 20+ checkout installs and `npm run dev` serves the dashboard on the documented local URL. | Isolated copied checkout: `npm ci --offline --ignore-scripts`; `npm rebuild better-sqlite3`; `npm start -- -p 3102`; GET `/` and `/harnesses` and confirm OpenEval branding. | PASS | — | — | PASS |
| US-002 | Dashboard summary | As an operator, I view the dashboard so that I can see recent sessions, runs, cases, usage, and outcome signals at a glance. | `/` returns the OpenEval dashboard with links into Collection, Timeline, Runs, and Cases without fabricating missing data. | Runtime GET `/`; `tests/summary.test.ts`; `tests/stats.test.ts`; `tests/format.test.ts`. | PASS | — | — | PASS |
| US-003 | Case library | As an evaluation author, I browse cases by category so that I can understand the available benchmark coverage. | `/cases` renders all shipped cases and category filters; the API returns the same valid case universe. | Runtime GET `/cases` and `/api/cases`; `tests/cases-schema.test.ts`; `npm run audit:accuracy`. | PASS | — | — | PASS |
| US-004 | Strict case authoring schema | As a case author, I receive validation errors for malformed definitions so that typos cannot silently weaken grading. | Unknown keys, invalid setup blocks, missing graders, and unknown grader types are rejected; every shipped case loads cleanly. | `tests/cases-schema.test.ts`; `OPENEVAL_DATA_ROOT=.test-data-user-stories npm run selftest`. | PASS | — | — | PASS |
| US-005 | Harness discovery and metadata | As an operator, I inspect installed agent CLIs so that I know which harnesses, versions, models, and capabilities are available. | `/harnesses` and `/api/harnesses` report descriptor-grounded availability and capability evidence; unknown capability remains unknown. | Runtime GET `/harnesses` and `/api/harnesses`; `npm run run:eval -- --list-harnesses`; `tests/harness-metadata.test.ts`. | PASS | — | — | PASS |
| US-006 | Descriptor harness extension | As an integrator, I add a `.harness.json` descriptor so that a compatible CLI can run without a custom adapter. | Descriptors validate, resolve binaries and flags, parse mapped JSON or JSONL fields, and preserve typed values. | `harnesses/hermes.harness.json`; `docs/harness-authoring.md`; `tests/harness-metadata.test.ts`; selftest generic-descriptor parser check. | PASS | — | — | PASS |
| US-007 | New-run builder | As an operator, I configure harnesses, models, runners, filters, samples, and parallelism so that I can launch a bounded evaluation. | `/runs/new` renders the current harness/model and case selection surfaces with category, tag, search, and explicit-case controls. | Runtime GET `/runs/new`; inspect `components/NewRunClient.tsx`; runtime GET `/api/models` and `/api/cases`. | PASS | — | — | PASS |
| US-008 | Run request validation | As an API client, I receive bounded errors for invalid run selections so that no accidental empty or unknown evaluation starts. | Empty, whitespace-only, unknown, or malformed case selections return HTTP 400 before a harness starts. | `tests/api-routes.test.ts` POST `/api/runs` cases. | PASS | — | — | PASS |
| US-009 | Evaluation CLI selection | As a CLI operator, I list harnesses and filter cases so that scripted evaluation runs select the intended workload. | `--list-harnesses` reports installed adapters; documented case, category, tag, harness, model, runner, sample, and parallel flags are parsed. | `npm run run:eval -- --list-harnesses`; inspect `lib/cli/run.ts`; `README.md` CLI contract. | PASS | — | — | PASS |
| US-010 | Real harness evaluation | As an operator, I run a shipped case through an installed agent harness so that I obtain a fresh end-to-end scored result. | A selected harness executes a real case, persists terminal case and run states, records usage, and produces grader evidence. | `npm run run:case -- swe-fix-fizzbuzz --harness ncode --json` launched a real local harness evaluation; run and case reached terminal error state with persisted runner/grader evidence. | BLOCKED | The installed ncode route reached the provider boundary but failed honestly because managed OAuth authentication had expired and could not be refreshed; usage remained zero. | — | BLOCKED |
| US-011 | Executor outcome precedence | As an operator, I receive honest failed versus error states so that agent mistakes are not confused with infrastructure failures. | Budget exhaustion, runner failure, grader infrastructure failure, forbidden checks, and partial agent text resolve to the documented bounded statuses. | `tests/executor.test.ts`; `tests/summary.test.ts`; `tests/telemetry.test.ts`. | PASS | — | — | PASS |
| US-012 | Run persistence and statistics | As an operator, I retain run history and trustworthy statistics so that comparisons survive process restarts. | SQLite persistence records runs, cases, events, usage, pass rates, pass-at-k, and stranded states without merging error classes. | `tests/run-lifecycle.test.ts`; `tests/stats.test.ts`; `tests/summary.test.ts`; runtime GET `/api/runs`. | PASS | — | — | PASS |
| US-013 | Cancellation and orphan recovery | As an operator, I stop a running evaluation and recover stale runs so that queued work does not remain falsely active. | Cancellation aborts queued work with an interim summary; orphan sweeping changes only stale non-terminal runs. | `tests/api-routes.test.ts` cancel cases; `tests/run-lifecycle.test.ts`. | PASS | — | — | PASS |
| US-014 | Server-sent run events | As an operator, I watch case progress without refreshing so that long evaluations remain observable. | `/api/runs/[id]/events/stream` emits ordered run events and terminates or reconnects safely when the run completes. | Isolated `OPENEVAL_DATA_ROOT` runtime smoke against `app/api/runs/[id]/events/stream/route.ts`: existing-run 200/SSE headers, retry and heartbeat frames, ordered `case_started`/`run_completed` frames, `Last-Event-ID` replay, terminal-run auto-close, and missing-run 404. | PASS | — | — | PASS |
| US-015 | Run history and detail | As an operator, I browse stored runs and open a run so that I can inspect outcomes and diagnostics. | `/runs` lists persisted runs; `/runs/[id]` renders summary, case states, filters, usage, grader output, and report controls. | Runtime GET `/runs`; runtime GET one stored `/runs/[id]`; runtime GET `/api/runs/[id]`. | PASS | — | — | PASS |
| US-016 | Case transcript and artifact detail | As an investigator, I inspect one evaluated case so that I can connect the score to its transcript, tool calls, grader evidence, and artifacts. | `/runs/[id]/case/[caseId]` renders the stored trace and only exposes artifacts associated with that case workdir. | Runtime GET one stored case-detail route; `tests/api-routes.test.ts` artifact cases; inspect `components/CaseDetailClient.tsx`. | PASS | — | — | PASS |
| US-017 | Artifact and workdir containment | As a local operator, I prevent evaluation inputs and artifacts from escaping their workdirs so that host files are not exposed or copied. | Traversal, absolute escapes, fixture symlinks, visual-input symlinks, and artifact symlinks outside the case workdir are rejected. | `tests/robustness.test.ts`; `tests/executor.test.ts`; `tests/grader.test.ts`; `tests/api-routes.test.ts`. | PASS | — | — | PASS |
| US-018 | Bench telemetry | As a performance investigator, I inspect per-case tokens, cost, duration, throughput, and tool timing so that I can diagnose evaluation efficiency. | `/runs/[id]/bench` renders persisted telemetry with explicit source-quality and coverage metadata. | Runtime GET one stored bench route; `tests/telemetry.test.ts`; `tests/pricing.test.ts`. | PASS | — | — | PASS |
| US-019 | Leaderboard and run comparison | As an operator, I compare harnesses and runs so that I can evaluate pass rate, cost, tokens, speed, and sample behavior side by side. | `/runs/leaderboard` and `/runs/compare` render without error and use bounded statistical summaries rather than raw aggregate guesses. | Runtime GET `/runs/leaderboard` and `/runs/compare`; `tests/stats.test.ts`; inspect `components/LeaderboardClient.tsx` and `components/CompareClient.tsx`. | PASS | — | — | PASS |
| US-020 | Weighted grader evaluation | As a benchmark author, I combine graders so that a case passes only when its weighted threshold and forbidden checks permit it. | Weights default to one, pass thresholds are applied, manual checks stay pending, unknown graders fail, and a failed forbidden grader vetoes a pass. | `tests/grader.test.ts` evaluation and registry cases; `docs/graders.md`. | PASS | — | — | PASS |
| US-021 | Deterministic grader library | As a benchmark author, I grade files, commands, JSON, diffs, transcripts, and tool-step traces so that results have reproducible proof. | Shipped file, checksum, command, test, JSON path, regex, diff, and step graders return bounded results and reject invalid paths. | `tests/grader.test.ts`; `OPENEVAL_DATA_ROOT=.test-data-user-stories npm run selftest`. | PASS | — | — | PASS |
| US-022 | Shell grader resource safety | As an operator, I bound grader commands so that noisy or hung checks cannot consume unbounded resources or appear successful. | Output is capped, timeouts kill the process group, zero-test success is rejected by default, and timeout exits remain nonzero. | `tests/grader.test.ts` output-cap, timeout, and `tests_pass` cases. | PASS | — | — | PASS |
| US-023 | External rubric judge execution | As a benchmark author, I use a separate judge backend so that subjective outputs can receive rubric evidence without grading themselves. | A configured external or local judge returns schema-valid scores, retries transient failures, and records permanent failures without silently passing. | Deterministic configured OpenRouter transport smoke (`JUDGE_HARNESS=openrouter`, stub model): schema-valid score persistence, two transient 429 retries, and missing-transcript permanent-failure recording; `tests/judge-backends.test.ts` covers score and failure-ledger contracts. | PASS | — | — | PASS |
| US-024 | Redacted report export | As an operator, I export a run so that I can share a Markdown report or portable bundle without exposing sensitive local text. | `report` emits a run report, honors `--redact`, rejects missing runs, and can write an explicit bundle destination. | `npm run report -- <stored-run-id> --redact`; `lib/report.ts`; `tests/redaction.test.ts`; `tests/review-pass.test.ts`. | PASS | — | — | PASS |
| US-025 | Repository selftest | As a maintainer, I validate cases, harness parsers, no-op baselines, known-bad scripts, and oracles so that benchmark regressions surface before release. | The selftest reports case-level evidence, rejects known-bad outputs where supplied, and distinguishes optional LLM-judge skips. | `OPENEVAL_DATA_ROOT=.test-data-user-stories npm run selftest`. | FIXED | The pipeline case's shared fixture had drifted so its corrected `stats.js` produced no diff for one weighted `git_diff_contains` grader even though the overall case passed. | Added immutable `fixtures/pipeline-regression` baseline and pointed `swe-pipeline-multi-fix` at it; the case now starts with all three intended bugs. | PASS |
| US-026 | Accuracy audit | As a benchmark maintainer, I audit evidence strength so that weak cases are visible instead of being treated as decision-grade. | The audit reports oracle, known-bad, deterministic, visual, LLM, and manual coverage and lists every weak case. | `npm run audit:accuracy`; `app/accuracy/page.tsx`; runtime GET `/accuracy`. | PASS | — | — | PASS |
| US-027 | Live trace intelligence | As an operator, I inspect recent local CLI sessions so that I can see measured, inferred, missing, and malformed usage with trace context. | `/live` and `/api/live` parse supported sources, preserve provenance, cache unchanged sessions, pair tools with results, and redact sensitive display text. | Runtime GET `/live` and `/api/live?limit=2`; `tests/live.test.ts`; `tests/live-cache.test.ts`; `tests/golden-parse.test.ts`. | PASS | — | — | PASS |
| US-028 | Honest unsupported-live state | As an operator, I see when a harness lacks live-trace support so that missing data is not shown as zero or success. | Unknown or unsupported harness requests return a bounded unavailable state and never borrow another harness's metadata. | `tests/live.test.ts` unsupported-harness and API-query cases; `tests/harness-metadata.test.ts`. | PASS | — | — | PASS |
| US-029 | Cross-harness Collection | As an operator, I aggregate sessions from all parseable harnesses so that local history survives source-log pruning and remains comparable. | `/collection` discovers sources, preserves archived summaries, deduplicates roots, aggregates models/tools/projects, and labels archived data and cost provenance. | Runtime GET `/collection` and `/api/collection`; `tests/collection.test.ts`; `tests/rollup-changepoints.test.ts`. | PASS | — | — | PASS |
| US-030 | Full-text transcript search | As an operator, I search conversations across harnesses so that I can find historical work without opening each source log. | Search indexes current and legacy Codex plus Hermes conversational text, replaces stale rows by extraction version, and returns bounded results. | Runtime GET `/api/collection/search?q=OpenEval&limit=3`; `tests/collection-search.test.ts`; `tests/live-cache.test.ts`. | PASS | — | — | PASS |
| US-031 | Read-only transcript viewer | As an operator, I open a Collection search result so that I can inspect the source conversation without editing it. | `/collection/session?file=...` renders a parseable session read-only with OpenEval framing and redacted path display. | Runtime search followed by GET of the returned `/collection/session` route; `tests/live.test.ts` transcript parsing. | PASS | — | — | PASS |
| US-032 | Timeline and impact analysis | As an operator, I inspect adoption markers and metric shifts so that I can reason about changes around skills, models, tools, and subagents. | `/collection/timeline` reports ordered markers, before/after medians, change points, judged coverage, and explicit confound or thin-sample warnings. | Runtime GET `/collection/timeline` and `/api/collection/timeline`; `tests/insights-timeline.test.ts`; `tests/insights-signals.test.ts`; `tests/rollup-changepoints.test.ts`. | PASS | — | — | PASS |
| US-033 | Timeline background judging | As an operator, I refine heuristic outcomes with persisted judge verdicts so that impact comparisons can become decision-grade over time. | Incremental and background judging persist verdicts, skip permanent failures, retry transient failures, and prefer sufficient judged samples. | Deterministic configured OpenRouter transport smoke: `judgePoints` persisted an incremental verdict, and `startJudgeAll` processed a three-session marker window to completion with three persisted judgments; `tests/insights-judge.test.ts` and `tests/judge-backends.test.ts`. | PASS | — | — | PASS |
| US-034 | Pricing and cost provenance | As an operator, I interpret cost estimates so that API-list-equivalent values are not confused with actual provider spend. | Known models use listed rates, family/fallback mappings are labeled, unknown models never become fake zero cost, token classes remain separate, and local model paths are sanitized. | `tests/pricing.test.ts`; `tests/collection.test.ts`; runtime GET `/api/collection`. | PASS | — | — | PASS |
| US-035 | Display redaction | As an operator, I hide local usernames, machine paths, and credential shapes so that screenshots and reports are safer to share. | Redaction masks deterministic secret shapes and named users, preserves useful suffixes, is idempotent, and applies consistently across display/report surfaces. | `tests/redaction.test.ts`; `docs/redaction.md`; `components/RedactToggle.tsx`; redacted report smoke. | PASS | — | — | PASS |
| US-036 | Local-first data and public-upload boundary | As a repository owner, I keep operator databases and traces local so that publishing the source does not publish personal run history. | Runtime databases, workdirs, transcripts, caches, and state remain ignored; the upload audit rejects tracked local-only files, private paths, and disallowed identity strings. | `git check-ignore` for runtime paths; `bash scripts/public-upload-audit.sh`; inspect `.gitignore`, `SECURITY.md`, and `docs/redaction.md`. | PASS | — | — | PASS |
| US-037 | Mutation request safety | As a local operator, I reject cross-site and DNS-rebound mutations so that a browser page cannot silently control OpenEval. | Mutation requests require a local or explicitly allowed host and same-origin or direct request context; cross-site and forged-host requests return HTTP 403. | `tests/middleware.test.ts`; inspect `middleware.ts`. | PASS | — | — | PASS |
| US-038 | Judge settings persistence | As an operator, I choose a judge source and model so that rubric and Timeline judging use the same local configuration. | Settings validate registered sources, persist atomically with mode 0600, leave no temp file, and defer to explicit environment overrides. | Runtime GET `/settings` and `/api/settings`; isolated `OPENEVAL_DATA_ROOT` save/read/mode smoke; `tests/judge-backends.test.ts`. | PASS | — | — | PASS |
| US-039 | Keyboard and responsive navigation | As a keyboard or small-screen operator, I navigate primary routes and controls so that the dashboard remains usable without a desktop pointer. | Mobile navigation, sidebar, command palette, tables, drawers, and primary actions remain reachable, labeled, focus-visible, and free of horizontal overflow at supported widths. | In-app browser smoke at 390×844 plus default viewport: open/close mobile navigation, inspect dialog/button names, open command palette with Ctrl/Cmd+K, verify focused search input, dismiss with Escape, and check document width. | FIXED | The mobile drawer close icon was an unlabeled button; the drawer and command palette also lacked explicit dialog semantics and the command input lacked an accessible label. | Added `aria-label`/`aria-modal`/`role="dialog"` contracts to the mobile drawer and command palette, labeled the close control and command search input, and marked the decorative close icon hidden. | PASS |
| US-040 | Production build | As a maintainer, I create a production bundle so that the current checkout is deployable beyond development mode. | `npm run build` completes with the current source and generated route types. | `npm run build` after the owner-managed dev server is stopped, followed by `npm start` route smoke. | FIXED | A pre-existing Next.js dev server owned the shared `.next` directory, so the first verification pass could not safely run the build. | The stray npm/dev process was closed; `npm run build` then completed in a persistent terminal session with compilation, type checking, page generation, traces, and route optimization all passing. | PASS |

| US-041 | Case file cache invalidation | As a case author, I edit a `.case.json` file while the dashboard is running so that the next load reflects the new definition without a process restart. | The case loader invalidates its in-process cache when the source mtime changes and surfaces validation errors from the edited file. | `tests/pending-gaps.test.ts` warms the case cache, changes source mtime, reloads, and observes the edited definition. | PASS | — | — | PASS |
| US-042 | Case filter combinations | As an evaluation operator, I filter by case id, category, tag, and difficulty so that a run contains exactly the intended benchmark slice. | `selectCases()` applies the requested filters together, sorts the resulting case ids deterministically, and returns no unrelated cases. | `tests/pending-gaps.test.ts` asserts combined id/category/tag/difficulty filtering and deterministic ordering. | PASS | — | — | PASS |
| US-043 | Case split and canary metadata | As a benchmark maintainer, I label public, held-out, and canary cases so that dataset policy can be represented without changing the case prompt. | Valid `split` and `canary` metadata loads, survives case API serialization, and remains available to accuracy and case-browser surfaces. | `tests/pending-gaps.test.ts` loads split/canary metadata and asserts it survives `GET /api/cases` serialization. | PASS | — | — | PASS |
| US-044 | Empty and fixture workdir setup | As a case author, I choose no setup or a fixture setup so that each case starts in an isolated prepared directory. | `none` creates an empty workdir; fixture setup copies the declared fixture into the run-specific case path without sharing another case's files. | `tests/pending-gaps.test.ts` runs none and fixture setup in isolated workdirs and verifies copied files do not share paths. | PASS | — | — | PASS |
| US-045 | Git-clone case setup | As a benchmark author, I use a repository source so that a case can evaluate an agent against a shallow cloned workdir. | `git-clone` requires a repository, clones into the prepared directory, and does not silently fall back to a host path. | `tests/pending-gaps.test.ts` creates a local git source, clones it into the run workdir, and verifies the repository commit and file contents. | PASS | — | — | PASS |
| US-046 | Per-case runner and budget overrides | As a benchmark author, I set turns, timeout, permission mode, model, extra args, and cost ceilings so that each case has bounded execution policy. | Case-level runner settings reach the harness context, and exceeded cost or turn budgets produce a failed case with `budget_exceeded`. | `tests/pending-gaps.test.ts` asserts case-level runner, permission, model, extra-argument, timeout, and cost override propagation; existing executor tests cover budget enforcement. | PASS | — | — | PASS |
| US-047 | Visual input and artifact contract | As a visual benchmark author, I declare image inputs and expected artifacts so that a harness receives the intended evidence contract. | Declared local images are attached only through the descriptor image flag, and visual artifacts contribute a visual evidence tier to accuracy reporting. | `tests/harness-metadata.test.ts` image flag cases; `npm run audit:accuracy`; `tests/executor.test.ts` visual-input containment case. | PASS | — | — | PASS |
| US-048 | No-op oracle baseline | As a benchmark maintainer, I run selftest so that a case with weak graders cannot pass without an agent change. | Selftest grades an unchanged baseline and fails when its score exceeds the declared `noop_max_score`. | `OPENEVAL_DATA_ROOT=.test-data-user-stories npm run selftest`; `docs/case-authoring.md` oracle contract. | PASS | — | — | PASS |
| US-049 | Known-bad oracle rejection | As a benchmark maintainer, I supply plausible-but-wrong outputs so that graders prove they reject common false solutions. | Every declared `known_bad` script runs in a fresh workdir and must fail the case; the accuracy audit reports coverage. | `OPENEVAL_DATA_ROOT=.test-data-user-stories npm run selftest`; `npm run audit:accuracy` reports 5/18 known-bad scripts. | PASS | — | — | PASS |
| US-050 | Reproducibility manifest | As an operator, I inspect a completed run's manifest so that I can recreate the case, harness, model, and grading context later. | Each run records a reproducibility manifest alongside its persisted run data and includes it in report output when available. | `tests/pending-gaps.test.ts` asserts run manifest defaults, repository identity, and persisted manifest fields used by report export. | PASS | — | — | PASS |
| US-051 | Model catalog aggregation | As an operator, I choose a model from a harness descriptor so that aliases, discovered local models, and the descriptor default appear in one catalog. | `GET /api/models` returns descriptor-backed aliases and discovered models without assigning another harness's catalog. | `tests/harness-metadata.test.ts` model catalog and default cases; runtime GET `/api/models`. | PASS | — | — | PASS |
| US-052 | Custom model id validation | As an API client, I submit a model id so that blank or control-filled values are rejected before command assembly. | Non-empty printable custom ids are accepted; blank and control-containing ids return validation errors. | `tests/harness-metadata.test.ts` model id validation; inspect `app/api/models/route.ts`. | PASS | — | — | PASS |
| US-053 | Default harness resolution | As an operator, I start a run without naming a harness so that OpenEval chooses the configured default or first registered descriptor deterministically. | `OPENEVAL_DEFAULT_HARNESS` wins when registered; otherwise the first registered descriptor becomes the default for runs and model APIs. | `tests/harness-metadata.test.ts` descriptor-default cases; `lib/adapters/registry.ts`. | PASS | — | — | PASS |
| US-054 | Harness binary override | As a local operator, I point a descriptor at a known binary path so that discovery and execution use the intended installation. | A descriptor `binEnvVar` override resolves the executable from the environment instead of guessing another binary. | `tests/harness-metadata.test.ts` binary-resolution case. | PASS | — | — | PASS |
| US-055 | Safe harness probes | As an operator, I inspect version and help output so that harness availability can be checked without spending model usage. | Probes use the descriptor's version and help arguments, report failures as metadata, and do not run a model prompt. | `tests/harness-metadata.test.ts` version/help probe cases; `npm run run:eval -- --list-harnesses`. | PASS | — | — | PASS |
| US-056 | Prompt mode assembly | As a harness integrator, I select arg, flag, stdin, or template prompt delivery so that a CLI receives the case instruction in its required form. | Descriptor command assembly sends the prompt exactly once using the selected mode and honors `{prompt}` template substitution. | `tests/pending-gaps.test.ts` asserts arg, flag, stdin, and template command assembly, including exactly-once prompt delivery. | PASS | — | — | PASS |
| US-057 | Permission argument fallback | As a harness integrator, I define permission modes so that the selected case policy reaches CLIs with either a mode-specific or wildcard argument set. | Mode-specific `permissionArgs` win, wildcard args are the fallback, and simple `permissionFlag` is supported when no map exists. | `tests/pending-gaps.test.ts` covers mode-specific, wildcard, and simple `permissionFlag` fallback branches. | PASS | — | — | PASS |
| US-058 | Extra args and environment propagation | As a harness integrator, I add descriptor environment and case extra arguments so that specialized CLIs can be configured without changing the runner. | `extraEnv` is merged into the child process, and case `extra_args` append unless the descriptor disables appending. | `tests/pending-gaps.test.ts` asserts descriptor environment, appended case args, and the descriptor opt-out branch. | PASS | — | — | PASS |
| US-059 | Generic JSONL field mapping | As a harness integrator, I map dotted or indexed JSON fields so that a compatible CLI becomes observable without custom parser code. | Mappings preserve false booleans, parse tool inputs when possible, emit tool events, and accumulate result fields from generic JSONL. | `tests/pending-gaps.test.ts` feeds mapped JSONL events and asserts typed false values, tool events, and final result fields. | PASS | — | — | PASS |
| US-060 | Parser format compatibility | As an operator, I use text, Claude stream JSON, Codex JSONL, or generic JSONL output so that supported harnesses produce a common runner result. | Each declared parser returns normalized events or a bounded error when no result can be parsed; legacy `output` values retain their documented mapping. | `tests/pending-gaps.test.ts` covers text and generic JSONL adapters alongside existing Claude/Codex golden parser tests. | PASS | — | — | PASS |
| US-061 | Codex rollout compatibility | As an operator, I inspect Codex App or CLI sessions from old and new rollout shapes so that history remains comparable across format changes. | New `session_meta` and legacy `response_item` records parse into equivalent transcript, usage, tool, and result fields where available. | `tests/golden-parse.test.ts` Codex NEW and OLD rollout cases; `tests/live.test.ts` Codex session cases. | PASS | — | — | PASS |
| US-062 | Claude project compatibility | As an operator, I inspect Claude project transcripts so that interactive sessions without an explicit result record still appear with honest inferred duration and turns. | Claude project JSONL is recognized, timestamps provide inferred timing, and missing result records do not fabricate measured usage. | `tests/golden-parse.test.ts` Claude interactive case; `tests/live.test.ts` duration and turn inference cases. | PASS | — | — | PASS |
| US-063 | Bounded runner output | As an operator, I run a noisy harness or grader so that retained diagnostics cannot exhaust memory or hide later events. | Line buffering and capped accumulation retain bounded output, preserve complete lines, and mark oversized fragments without unbounded growth. | `tests/spawn.test.ts` `appendCapped` and `drainLineBuffer` cases; `tests/grader.test.ts` output-cap case. | PASS | — | — | PASS |
| US-064 | Process-group timeout cleanup | As an operator, I set a timeout so that a hung harness and its child processes are terminated rather than left running. | Timeout kills the whole process group, returns a nonzero outcome, and does not report a successful result. | `tests/grader.test.ts` process-group timeout case; `tests/spawn.test.ts` timeout and spawn cases. | PASS | — | — | PASS |
| US-065 | Runner diagnostic separation | As an evaluator, I receive a runner failure so that stderr and timeout diagnostics cannot masquerade as the agent's final answer. | Error diagnostics live in `error_msg` or runner detail; `finalText` contains only genuine parsed agent text and cannot satisfy a grader from a stack trace. | `tests/executor.test.ts` runner-failure and regex-on-error cases; `tests/spawn.test.ts` Codex diagnostic case. | PASS | — | — | PASS |
| US-066 | Tmux evaluation mode | As an operator, I choose the tmux runner so that a long evaluation can be observed or attached to while it executes. | `--runner tmux` starts each harness command in the documented attachable session shape and returns terminal status to OpenEval. | `tests/pending-gaps.test.ts` runs a short local harness through the tmux runner and verifies terminal output is retained; `lib/runner/tmux.ts` now persists fast-session logs before cleanup. | FIXED | Short-lived tmux sessions could exit before the prior polling loop observed their output, producing an empty result. | Added a bounded tee-backed session log and regression coverage for fast local harnesses. | PASS |
| US-067 | Safe JSON persistence fallback | As an operator, I encounter a corrupt optional JSON field so that the dashboard still loads without crashing the entire run view. | `safeParse` returns the supplied fallback for null, undefined, or corrupt JSON while valid JSON remains intact. | `tests/review-pass.test.ts` `safeParse` cases. | PASS | — | — | PASS |
| US-068 | Ordered run event persistence | As an operator, I inspect a run timeline so that case-start, tool, grading, and completion events remain ordered after persistence. | SQLite events retain increasing ids and payloads, and the run detail/SSE surfaces can read them by run and optional case. | `tests/run-lifecycle.test.ts`; `tests/api-routes.test.ts`; focused SSE smoke recorded in the phase log. | PASS | — | — | PASS |
| US-069 | Lightweight run detail API | As a dashboard client, I request a lightweight run record so that list and comparison views avoid loading full transcripts unnecessarily. | `GET /api/runs/[id]?lite=1` returns the bounded run/case shape intended for summary clients without requiring full case artifacts. | `tests/api-routes.test.ts` asserts lite responses omit grader payloads and cap runner text/tool fields while retaining summary state. | PASS | — | — | PASS |
| US-070 | Telemetry API quality labels | As a performance investigator, I request run telemetry so that measured, inferred, missing, and tool-timing coverage remain visible in the response. | `GET /api/runs/[id]/telemetry` returns case-level and run-level telemetry with source-quality labels and no fake zero for missing measurements. | `tests/telemetry.test.ts`; `tests/summary.test.ts`; `lib/telemetry.ts`; runtime bench route smoke. | PASS | — | — | PASS |
| US-071 | Report API formats | As an operator, I download a run report so that Markdown and portable bundle formats are available through the dashboard API. | `GET /api/runs/[id]/report` returns a redacted Markdown report or bundle with the correct content disposition and rejects an unknown run. | `tests/api-routes.test.ts` exercises Markdown and `?bundle=1` API responses, content disposition, gzip type, redaction, and missing-run handling. | FIXED | The bundle API path was implemented but lacked route-level verification. | Added the portable archive response and API regression coverage while preserving the existing Markdown contract. | PASS |
| US-072 | Portable report bundle contents | As a reviewer, I open a portable report bundle so that summary, manifest, and per-case evidence travel together without the source database. | Bundle output contains `report.md`, `manifest.json`, `summary.json`, and bounded per-case JSON artifacts under the requested destination. | `tests/api-routes.test.ts` generates a fresh gzip archive and inspects its tar listing for report, manifest, summary, and case evidence files. | FIXED | The bundle writer was covered by library tests but not by a fresh API-generated archive inspection. | Added a portable API bundle regression and archive-content assertion. | PASS |
| US-073 | CSV and JSON client export | As an operator, I export tabular dashboard data so that I can continue analysis outside the browser. | CSV cells quote and double embedded quotes, JSON exports preserve the supplied data, and downloads use the requested filename and MIME type. | Run detail browser snapshot shows named `Export CSV` and `Export JSON` controls wired to `lib/export.ts`; `tests/review-pass.test.ts` covers RFC-4180 quoting. | FIXED | Export helpers existed without dashboard controls, leaving the feature unreachable from the run detail surface. | Added run-level CSV and JSON export controls with stable filenames and bounded case fields. | PASS |
| US-074 | Artifact preview classification | As an investigator, I open a case artifact so that SVG, HTML, and text files receive an appropriate safe preview treatment. | Artifact classification recognizes supported content kinds, sanitizes or constrains preview behavior, and does not execute arbitrary outside-workdir files. | In-app browser smoke on a synthetic HTML artifact shows `Live artifact preview` and a sandboxed iframe (`sandbox=""`, title `Preview of preview.html`) containing the expected safe document; containment tests cover outside-workdir rejection. | PASS | — | — | PASS |
| US-075 | Missing and unsafe artifact handling | As an investigator, I request an artifact that is missing, outside the case workdir, or symlinked elsewhere so that the dashboard refuses unsafe access. | Requests without a valid absolute workdir, traversal paths, and escaping symlinks return bounded errors rather than host file contents. | `tests/api-routes.test.ts` artifact cases; `tests/robustness.test.ts`; `tests/grader.test.ts` path containment cases. | PASS | — | — | PASS |
| US-076 | Route error boundaries | As an operator, I hit a route or data-loading failure so that the dashboard shows a bounded recovery surface instead of a blank page. | App, runs, cases, accuracy, and live error boundaries render their route-specific fallback UI and preserve navigation context. | All five route-specific `error.tsx` boundaries import the shared recovery component with route-specific titles; production typecheck/build verification confirms the boundaries compile into the App Router. | PASS | — | — | PASS |
| US-077 | Empty-state guidance | As an operator with no runs or no discovered sessions, I open the dashboard so that I receive an actionable explanation instead of misleading zeros. | Empty collections, absent live roots, and no-run views identify the missing data state and link to the next useful action without claiming activity occurred. | Clean-data dashboard browser snapshot showed `No runs yet` and the actionable `Start your first evaluation` link; empty collection/live tests cover absent-source behavior. | PASS | — | — | PASS |
| US-078 | Local appearance preference | As an operator, I change the dashboard appearance so that the interface remains readable across sessions. | Theme and display controls apply the selected local preference without changing evaluation data or requiring a provider connection. | In-app browser smoke toggled to light mode, reloaded, and observed `html.light` plus `Switch to dark mode`; the same run-detail hero was retested in both themes after the contrast fix. | PASS | — | — | PASS |
| US-079 | New-run defaults persistence | As an operator, I reuse my preferred run controls so that creating the next evaluation does not require re-entering local defaults. | Settings client state stores and reads run defaults under the OpenEval local-storage key and keeps absent or malformed values at safe defaults. | In-app browser settings smoke changed defaults to parallel `3` and samples `2`, saved, reloaded, and observed both values retained. | PASS | — | — | PASS |
| US-080 | Settings API validation and file safety | As an operator, I save judge settings so that only registered sources and models are persisted with restrictive local permissions. | Settings round-trip, environment overrides remain authoritative, the file uses mode `0600`, writes are atomic, and temporary files are removed. | Isolated settings persistence smoke; `tests/judge-backends.test.ts`; runtime GET `/api/settings`; `lib/settings.ts`. | PASS | — | — | PASS |
| US-081 | Transcript source recognition | As an operator, I scan a mixed source directory so that only transcript-shaped JSONL or JSON session files enter Collection. | The scanner accepts supported transcript heads, rejects unrelated content, and limits large-file recognition work to the configured head. | `tests/collection.test.ts` transcript-shape and large-file cases. | PASS | — | — | PASS |
| US-082 | Missing-source resilience | As an operator, a configured trace root disappears so that Collection and live views remain usable and report an empty or unavailable source instead of throwing. | Absent roots yield bounded empty aggregates or explicit unsupported warnings, while other sources continue to scan. | `tests/collection.test.ts` absent-root case; `tests/live.test.ts` unsupported-harness cases. | PASS | — | — | PASS |
| US-083 | Archived session retention | As an operator, a harness prunes an old log file so that Collection still preserves the cached session as archived history. | Cached sessions whose source files disappear remain queryable, are marked archived, and are not silently counted as current files. | `tests/collection.test.ts` archived-session cases; `tests/live-cache.test.ts`; runtime `/api/collection` smoke. | PASS | — | — | PASS |
| US-084 | Collection model and tool rollups | As an operator, I inspect cross-harness usage so that model totals and tool counts are merged, ranked, and bounded without duplicate source entries. | Same-model totals merge across sources, tools sort by calls, lists are capped, and local model paths are sanitized with pricing evidence. | `tests/collection.test.ts` model/tool rollup cases; `tests/pricing.test.ts`; runtime `/collection`. | PASS | — | — | PASS |
| US-085 | Weekly rollup and DST handling | As an operator, I review weekly usage so that week buckets and activity remain correct across calendar and daylight-saving transitions. | Sessions bucket into Monday-first weeks, project ranking is deterministic, and sessions on the far side of a DST transition remain in the correct bucket. | `tests/rollup-changepoints.test.ts` week, project, and DST cases; `components/CollectionCharts.tsx`. | PASS | — | — | PASS |
| US-086 | Collection activity heatmap | As an operator, I inspect when sessions happen so that weekday and hour patterns are visible without guessing from totals. | The heatmap counts session starts by weekday and hour using a stable Monday-first layout and preserves the total-session denominator. | `tests/rollup-changepoints.test.ts` heatmap case; `components/CollectionCharts.tsx`; runtime `/collection` smoke. | PASS | — | — | PASS |
| US-087 | Transcript FTS index rebuild | As an operator, I rebuild the Collection search index so that newly discovered or re-parsed transcripts become searchable. | `POST /api/collection/search/index` upserts normalized conversational text, records file metadata, and returns a bounded index result without duplicating documents. | Live `POST /api/collection/search/index` smoke returned `{"indexed":1,"remaining":1491,"total":1492}` with HTTP 200 against the isolated dev data root. | PASS | — | — | PASS |
| US-088 | Parser-version cache invalidation | As a maintainer, I change a trace parser so that old cached interpretations are not reused under a new output contract. | Cache rows stamped with an older `PARSER_VERSION` are ignored and re-parsed; deleting the cache remains a safe full-rebuild fallback. | `lib/live-cache.ts` parser-version contract; `tests/live-cache.test.ts`; `tests/golden-parse.test.ts`. | PASS | — | — | PASS |
| US-089 | Judge-session exclusion | As an operator, I run rubric or Timeline judging so that the judge's own CLI session does not pollute Collection usage or adoption metrics. | Sessions beginning with the judge prompt marker are dropped by supported parsers while evaluated user sessions remain searchable. | `tests/collection.test.ts` and `tests/golden-parse.test.ts` judge-marker cases; `docs/architecture.md`. | PASS | — | — | PASS |
| US-090 | Optional PII redaction fallback | As an operator, I share a report on a machine without the optional PII model so that deterministic path and secret redaction still protects the text. | Secrets and local paths are redacted by default; the optional Rampart layer is used only when available, and absence leaves deterministic redaction intact. | `tests/redaction.test.ts` default and Rampart-unavailable cases; `docs/redaction.md`; `lib/redaction.ts`. | PASS | — | — | PASS |

## Phase log

### 2026-07-16 — Catalog

- Read `README.md`, `CONTRIBUTING.md`, `CHANGELOG.md`, `SECURITY.md`, `SUPPORT.md`, `state.yaml`, package scripts, product documentation, App Router pages and API routes, components, adapters, graders, runners, cases, harness descriptors, fixtures, scripts, and all test files.
- Cataloged 90 user-observable behaviors across setup, evaluation execution, persistence, grading, adapters, parsers, dashboards, live traces, Collection, search, Timeline, reporting, settings, privacy, request safety, accessibility, and production readiness.
- Preserved unrelated working-tree changes; this task changes the canonical ledger, the ignored `state.yaml` receipt, and the navigation accessibility contracts in `components/MobileNav.tsx` and `components/CommandPalette.tsx`.

### 2026-07-16 — Automated verification

- `npm test`: PASS — 249 tests, 249 passed, 0 failed, 0 skipped.
- `npm run typecheck`: PASS.
- `npm run lint`: PASS — zero warnings or errors; Next.js printed its upstream `next lint` deprecation notice.
- `OPENEVAL_DATA_ROOT=.test-data-user-stories npm run selftest`: PASS — 23 case checks passed, 0 failed; six optional LLM-judge checks skipped because `--with-llm-judge` was not enabled.
- Selftest nuance: `swe-pipeline-multi-fix` showed one failed weighted `git_diff_contains` sub-check while the case still satisfied its pass threshold. The overall selftest passed; this does not mean every individual grader passed.
- `npm run audit:accuracy`: PASS — 18/18 cases have oracle coverage and deterministic or trace coverage; 5/18 have known-bad scripts; 13 cases are explicitly listed as weak for missing known-bad rejection.
- `bash scripts/public-upload-audit.sh`: PASS — no tracked local-only files, disallowed public identity, or private machine path; two intentional adversarial secret-like fixtures remain inventoried.
- `npm run run:eval -- --list-harnesses`: PASS — all four discovered harness binaries were available.
- Isolated settings persistence smoke with a temporary `OPENEVAL_DATA_ROOT`: PASS — save/read round-trip matched, file mode was `0600`, and no temporary file remained.
- Story-expansion verification: `npm test` PASS — 249 tests passed, 0 failed; `npm run typecheck` PASS; `npm run lint` PASS with zero warnings or errors; `npm run audit:accuracy` completed with 18/18 cases covered by oracle and deterministic or trace evidence; `bash scripts/public-upload-audit.sh` PASS.

### 2026-07-16 — Story expansion

- Added `US-041` through `US-090` without renumbering or rewriting the original 40 rows.
- Added 27 `PASS` rows from current unit, integration, isolated persistence, API, parser, security, redaction, and accuracy evidence.
- Added 23 follow-up rows; all but the provider-authenticated real-harness smoke now have focused evidence. No row was marked `FAIL` without current evidence.

### 2026-07-16 — Runtime verification

- Reused the already-running OpenEval development server without stopping or restarting it.
- HTTP 200 with OpenEval branding: `/`, `/runs`, `/runs/new`, `/runs/leaderboard`, `/runs/compare`, `/harnesses`, `/accuracy`, `/live`, `/collection`, `/collection/timeline`, `/cases`, and `/settings`.
- HTTP 200 JSON: `/api/cases`, `/api/harnesses`, `/api/live?limit=2`, `/api/collection`, `/api/collection/timeline`, `/api/settings`, and `/api/models`.
- HTTP 200 with OpenEval branding for one stored `/runs/[id]`, `/runs/[id]/bench`, and `/runs/[id]/case/[caseId]` route.
- Search returned bounded results, and the corresponding read-only Collection session viewer returned HTTP 200 with OpenEval branding.
- `npm run report -- <stored-run-id> --redact`: PASS — emitted a redacted Markdown report for an existing run.

### 2026-07-16 — Setup and interactive navigation retest

- Clean-install smoke in an isolated copied checkout: `npm ci --offline --ignore-scripts` completed, `npm rebuild better-sqlite3` completed, and a fresh `npm start -- -p 3102` served HTTP 200 with OpenEval branding for `/` and `/harnesses`. The initial `--ignore-scripts` native-binding failure was an intentionally incomplete install mode; the documented dependency rebuild path passed, so US-001 is PASS rather than a product fix.
- In-app browser responsive smoke at 390×844: mobile navigation opened and closed; the drawer exposed `role="dialog"`, `aria-modal="true"`, and a named close button; Ctrl/Cmd+K opened a named command-palette dialog with a focused, named search input; Escape dismissed it; document width did not exceed the viewport. The temporary viewport override was reset and the browser session was finalized.
- `npm run build`: PASS after the accessibility patch; compilation, type checking, page generation, traces, and route optimization completed.
- `npm test`, `npm run typecheck`, and `npm run lint`: PASS — 249 tests passed; no type errors; no lint warnings or errors.

### 2026-07-16 — Case-quality fix and retest

- Found that the shared `fixtures/pipeline` had a user-owned variance fix, so the pipeline oracle could not produce a `stats.js` diff. Added the dedicated `fixtures/pipeline-regression` baseline without overwriting the shared fixture edit and updated `swe-pipeline-multi-fix.case.json` to use it.
- Retest: `OPENEVAL_DATA_ROOT=.test-data-user-stories npm run selftest` — PASS, 23 case checks passed, 0 failed; `swe-pipeline-multi-fix` now reports `tests_pass:ok`, both `git_diff_contains:ok`, and `files_unchanged:ok`.

### 2026-07-16 — Initial-pass limitations (superseded by later checks)

- The initial pass did not launch a fresh real-harness evaluation; the later bounded ncode smoke did launch one and is recorded as BLOCKED on expired managed OAuth in the pending-gap section below.
- Did not exercise run-event SSE because no isolated evaluation was started.
- Did not enable live LLM-judge selftests or a Timeline background judge job.
- Browser-console cleanliness was not independently audited; the interactive keyboard/focus/responsive smoke covered the primary navigation and command-palette paths only.
- The initial pass did not run `npm run build` because a pre-existing dev server owned the same `.next` directory. After that process was closed, the production build and port-3101 route smoke passed; the original blocker is retained in US-040's Error and Fix columns.

### 2026-07-16 — SSE focused runtime smoke

- An isolated `OPENEVAL_DATA_ROOT` `tsx` smoke created synthetic running and terminal runs and invoked `GET /api/runs/[id]/events/stream` directly. The stream returned HTTP 200 with `text/event-stream`, `retry: 2000`, a heartbeat, ordered event IDs, and a terminal `run_completed` frame that closed the body.
- A reconnect-style request with `Last-Event-ID` omitted the acknowledged event, delivered the next event and terminal event, and closed. A completed run with no completion event auto-closed without fabricating an event; an unknown run returned HTTP 404.

### 2026-07-16 — Judge backend and background smoke

- A deterministic `JUDGE_HARNESS=openrouter` smoke replaced the transport with an in-process stub, so no provider request or spend occurred. The configured backend returned a schema-valid `0.85` verdict after two transient 429 responses; the verdict persisted with the selected judge name.
- The same smoke sent a missing transcript through the incremental queue and verified a permanent failure ledger entry rather than a silent pass. It then started a three-session marker-window background job and observed `done=3`, `judged=3`, `failed=0` with persisted verdicts. Existing judge-backend and Timeline unit suites also passed.

### 2026-07-16 — Pending-gap verification and remediation

- `OPENEVAL_DATA_ROOT=.gap-test-data node --import tsx --test tests/pending-gaps.test.ts tests/api-routes.test.ts`: PASS — 21 tests passed, 0 failed. This covered case cache invalidation, combined filters, split/canary serialization, none/fixture/git-clone setup, per-case overrides, manifest defaults, prompt and permission assembly, extra args/environment, generic JSONL mapping, parser compatibility, tmux, lite run detail, report formats, and portable bundle contents.
- `POST /api/collection/search/index` live smoke: PASS — HTTP 200 with a bounded `{ indexed, remaining, total }` result.
- Real harness boundary: `npm run run:case -- swe-fix-fizzbuzz --harness ncode --json` reached the installed ncode runner and persisted terminal error/grader evidence, but the provider returned `Managed OAuth authentication expired and could not be refreshed`; usage remained zero. US-010 is BLOCKED pending local provider re-authentication, not treated as a product failure.
- In-app browser smoke: PASS — settings defaults survived reload; light-theme preference survived reload; clean dashboard empty-state guidance was visible; artifact preview rendered expected HTML inside a sandboxed iframe; run detail exposed Export CSV and Export JSON controls.
- Contrast remediation: replaced the run hero's fixed dark gradient with theme-aware CSS variables and strengthened run-id/model foreground contrast. Both light and dark computed-style checks now use readable theme colors.
- Final validation: `npm test` PASS — 259 tests passed, 0 failed; `npm run typecheck` PASS; `npm run lint` PASS with zero warnings or errors; `npm run build` PASS; `npm run audit:accuracy` PASS; `bash scripts/public-upload-audit.sh` PASS; `git diff --check` PASS.

## Summary by status

| Status | Count |
|--------|------:|
| PASS | 82 |
| FIXED | 7 |
| FAIL | 0 |
| PENDING | 0 |
| BLOCKED | 1 |
| **Total** | **90** |

### Warnings and known limitations

- The ledger is not a declaration that the entire release is provider-green: one real-harness smoke remains blocked on expired local managed OAuth credentials.
- Accuracy audit warnings are real coverage gaps, not test failures: 13 of 18 cases lack known-bad rejection scripts.
- The default selftest deliberately omitted six external LLM-judge checks.
- Judge backend and background-job runtime evidence used a deterministic in-process transport stub; live provider credentials and spend were intentionally not exercised.
- Runtime route smokes prove server responses and branding, not full client-side interaction, visual layout, keyboard behavior, or console cleanliness.
- Cost values are estimates with provenance and are not claims about actual subscription or provider spend.

### Non-bugs observed

- A case may pass its configured weighted threshold even when one non-forbidden sub-grader fails; the selftest preserves that evidence instead of rewriting it as an all-grader pass.
- Missing or unsupported live usage remains missing or unavailable rather than being displayed as zero.
- Intentional secret-like fixture files are adversarial test data and remain covered by the public-upload review inventory.
